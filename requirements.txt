# Production Python dependencies (ranges for human edits).
# Runtime installs MUST use requirements.lock for a reproducible set.
# Install lock: pip install -r requirements.lock
# Refresh lock: ./scripts/refresh_requirements_lock.sh

# Bot
aiogram>=3.7,<4.0

# Userbot
telethon>=1.36,<2.0
cryptg>=0.4,<1.0

# Database
SQLAlchemy>=2.0,<3.0
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0
greenlet>=3.0,<4.0

# Redis
redis>=5.0,<6.0

# HTTP
aiohttp>=3.9,<4.0

# Admin panel
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
itsdangerous>=2.1,<3.0

# Config
pydantic-settings>=2.2,<3.0

# NLP
pymorphy3>=1.2,<2.0
pymorphy3-dicts-ru>=2.4,<3.0
setuptools>=68,<81

# Monitoring
sentry-sdk>=2.0,<3.0
