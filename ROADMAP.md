# ROADMAP.md — План разработки по фазам

Детальные контрольные точки каждой фазы. В CLAUDE.md — только таблица статусов и текущая фаза (раздел 8).

---

## Таблица фаз

| Фаза | Название | Статус |
|---|---|---|
| Фаза 0 | Подготовка сервера (Docker, Python, firewall, токены) | ✅ |
| Фаза 1 | Скелет проекта, бот отвечает на /start | ✅ |
| Фаза 2 | БД, модели (v2 каталог), Alembic, seed, базовые тесты | ✅ |
| Фаза 3 | Inline-кнопки: управление словами/каналами + FSM-навигация по каталогу | ✅ |
| Фаза 4 | Userbot + Telethon, classifier.py, перехват сообщений | ✅ |
| Фаза 5 | Очередь + рассыльщик уведомлений + дымовые тесты | ✅ |
| Фаза 6а | Веб-панель: базовый CRUD каталога (SQLAdmin) | ✅ |
| Фаза 6б | Веб-панель: дашборд аналитики (KPI, графики, источники) | ✅ |
| Фаза 6в | Веб-панель: live-чат с пользователями (WebSocket) | ✅ |
| Фаза 6г | Веб-панель: рассылки и напоминания (broadcast + reminders) | ✅ |
| Фаза 7 | Оплата (Stars + CryptoBot) и подписки | ✅ |
| Фаза 8 | Надёжность: Docker, автоперезапуск, бэкапы, Sentry, heartbeat | ✅ |
| Фаза 9 | Пул userbot'ов и масштабирование (после первых юзеров) | ✅ |

---

## Контрольные точки

### Фаза 0 — Подготовка сервера

- [ ] SSH работает по ключу (не по паролю)
- [ ] `docker --version` и `python3 --version` отвечают
- [ ] Swap 2GB настроен
- [ ] Токены получены: BOT_TOKEN, USERBOT_API_ID, USERBOT_API_HASH
- [ ] Контейнеры стартуют с memory limits
- [ ] UFW: только порты 22 (и 80/443 если веб)

### Фаза 1 — Скелет проекта

- [ ] `/start` в Telegram → бот отвечает
- [ ] Контейнеры живут после `docker compose up`
- [ ] i18n работает (ru/en) — тексты из `locales/`
- [ ] Кнопки выбора языка на приветственном экране
- [ ] Onboarding wizard запускается при первом `/start`

### Фаза 2 — БД и модели

- [ ] После `/start` в таблице `users` появляется запись
- [ ] Все 15 таблиц созданы миграцией Alembic
- [ ] Миграция обратима (downgrade работает)
- [ ] Seed-данные загружены (segment_seed.md → seed_catalog.py)
- [ ] Unit-тесты на модели и keyword_matches проходят
- [ ] Индексы созданы (особенно idx_user_sub_lookup)

### Фаза 3 — Inline-кнопки

- [ ] Главное меню из 9 кнопок работает
- [ ] Добавление/удаление слов — лимиты Free срабатывают
- [ ] Добавление/удаление каналов — лимиты Free срабатывают
- [ ] Приватный канал → сообщение + алерт владельцу
- [ ] FSM-воронка: направление → страна → география → города → подтверждение
- [ ] Trial активируется при первом прохождении воронки
- [ ] Экран триала → «Показать 10 последних запросов»
- [ ] Экран ⚙️ Настройки: дайджест уведомлений, игнор-лист, CSV-экспорт (Business)

### Фаза 4 — Userbot + Classifier

- [ ] Userbot авторизован (session-файл)
- [ ] Poller слушает каналы без вступления (GetHistory, умный по tier'ам)
- [ ] Импорт спаршенных списков каналов через админку
- [ ] `classifier.classify_message()` отрабатывает на каждом сообщении
- [ ] Трёхпроходный алгоритм работает (demand → stop → structural)
- [ ] Личные keywords матчатся независимо
- [ ] Срочные (🔥) детектятся
- [ ] Результат классификации попадает в лог

### Фаза 5 — Очередь и рассыльщик

- [ ] Полный путь: сообщение → классификация → find_users → дедупликация → уведомление
- [ ] Уведомление приходит за ~2 сек
- [ ] Free-формат: контакты скрыты, кнопка «Активировать подписку»
- [ ] Paid-формат: полный, кнопки «Ответить»
- [ ] Throttle 25/сек работает
- [ ] Дымовой тест проходит
- [ ] Heartbeat 15 минут работает

### Фаза 6а — Веб-панель CRUD

- [ ] Админка открывается по SSH-туннелю (127.0.0.1:8001)
- [ ] Авторизация по паролю работает
- [ ] CRUD: страны, города, направления, ключевые слова, каналы
- [ ] M:N назначения: channel×segment, channel×city
- [ ] Импорт спаршенных списков каналов (админка + авто-матчинг страны/города)
- [ ] Изменения в админке сразу видны в боте

### Фаза 6б — Дашборд аналитики

- [ ] KPI-карточки: всего пользователей, новых, оплат, продлений
- [ ] Распределение по тарифам (Free/Pro/Business/Trial)
- [ ] 3 графика Chart.js: пользователи по дням, воронка, доход
- [ ] Источники пользователей (direct/referral/search/ad)

### Фаза 6в — Live-чат

- [ ] Список диалогов с индикаторами 🔴
- [ ] WebSocket-переписка в реальном времени
- [ ] История сохраняется в support_messages
- [ ] Пользователь пишет боту → сообщение в админку → ответ через Bot API

### Фаза 6г — Рассылки и напоминания

- [ ] Broadcast: выборка по тарифу/источнику, превью, статус
- [ ] RATE_SLEEP=1/30, двойной запуск заблокирован
- [ ] Напоминания: триал истёк (дни 1,3,7), подписка истекла (дни 1,3,7), неактивность (дни 14,28)
- [ ] Кнопка «Не присылать больше» работает
- [ ] Периодические сообщения Free: 📊 Итоги недели (Пн 10:00), 🌱 Новое в нише (Чт, раз в 2 нед.), 📈 Твой месяц (1-е число). Таблица periodic_prefs. Чекбоксы в Настройках

### Фаза 7 — Оплата

- [ ] Тестовая оплата Stars → план меняется на Pro
- [ ] Тестовая оплата CryptoBot → план меняется на Pro
- [ ] Лимиты расширяются соответственно тарифу
- [ ] Trial → истечение → авто-понижение до Free
- [ ] Реферальный бонус начисляется после оплаты referral
- [ ] End-of-day отчёт работает

### Фаза 8 — Надёжность

- [ ] Перезагрузка сервера → всё поднимается само (restart: unless-stopped)
- [ ] `pg_dump` cron работает, ротация 7 дней
- [ ] Тестовое восстановление БД из бэкапа
- [ ] Sentry шлёт ошибки
- [ ] Heartbeat алерты приходят владельцу
- [ ] Swap выдерживает пиковую нагрузку

### Фаза 9 — Масштабирование

- [ ] Несколько userbot'ов в пуле
- [ ] Новый канал → наименее загруженный аккаунт
- [ ] При падении аккаунта — авто-перераспределение каналов

---

## План развития (после Фазы 9)

### 📋 Задача: Второй userbot-аккаунт

**Статус:** ⏳ отложена (~через неделю, ожидается второй номер)

**Зачем:** распределить нагрузку между двумя Telegram-аккаунтами. Аккаунт 1 слушает каналы (poller), аккаунт 2 ищет новые каналы (discovery). Если один ловит FloodWait — второй продолжает работу.

**Кодовая база готова** — изменения внесены 2026-06-30, осталось получить номер и авторизовать.

#### Что уже сделано (код)

| Файл | Изменение |
|---|---|
| `app/config.py` | Поля `userbot_2_api_id`, `userbot_2_api_hash`, `userbot_2_phone` + метод `get_userbot_creds(account_id)` |
| `.env.example` | Документация для аккаунта 2 |
| `app/userbot/auth.py` | Поддержка `--session userbot2`, авто-выбор кредов под сессию |
| `app/userbot/pool.py` | `UserbotAccount.__init__` и `.start()` берут креды через `get_userbot_creds(account_id)` |
| `app/userbot/discovery.py` | `_discovery_session()` — если `userbot2.session` существует, discovery использует аккаунт 2; иначе падает обратно на аккаунт 1 |

#### Логика распределения

- **Аккаунт 1** (`userbot.session`): только прослушивание каналов (poller)
- **Аккаунт 2** (`userbot2.session`): только поиск новых каналов (discovery)
- Если аккаунта 2 нет — discovery падает обратно на аккаунт 1
- Pool сам подхватит обе сессии и распределит каналы между ними round-robin
- Rate limiter общий (3 сек между любыми API-вызовами, независимо от аккаунта)
- Circuit breaker срабатывает при любом FloodWait на любом аккаунте — блокирует оба

#### Инструкция (что осталось сделать)

**Шаг 1. Получить второй номер.** Любая SIM с возможностью принять SMS.

**Шаг 2. Зарегистрировать отдельное приложение Telegram:**
1. Зайти на https://my.telegram.org/apps с аккаунта второго номера
2. Создать новое приложение (App title: LeadHunter2, Platform: Desktop)
3. Скопировать `api_id` и `api_hash`

**Шаг 3. Добавить креды в `.env`:**
```env
USERBOT_2_API_ID=НОВЫЙ_API_ID
USERBOT_2_API_HASH=НОВЫЙ_API_HASH
USERBOT_2_PHONE=+7XXXXXXXXXX
```

**Шаг 4. Авторизовать аккаунт 2:**
```bash
cd /opt/LeadHunter
docker compose run --rm -it worker python -m app.userbot.auth --session userbot2
# Ввести код из SMS
```

**Шаг 5. Перезапустить worker:**
```bash
docker compose up -d worker
```

**Шаг 6. Проверить:**
```bash
docker compose logs worker | grep "Pool initialized"
# Должно быть: Pool initialized: 2 healthy accounts
docker compose logs worker | grep "Discovery using session"
# Должно быть: Discovery using session 'userbot2' (api_id=...)
```

#### Критерий готовности
- [ ] `docker compose logs worker` → `Pool initialized: 2 healthy accounts`
- [ ] Discovery использует `userbot2.session`, а не `userbot`
- [ ] Poller распределяет каналы между обоими аккаунтами
- [ ] При остановке одного аккаунта — каналы перераспределяются на оставшийся
