# LeadHunter — Session Handoff

**Generated:** 2026-07-05 13:21 UTC  
**Branch:** `main`  
**Origin:** `13b601d` (clean — up to date with origin/main)  
**Alembic head:** `manrev01`  
**Worker:** RUNNING, both accounts healthy

---

## Summary

Data-heavy session. Closed the zero-city country gap (24→8 countries), laid groundwork for discovery expansion. 8+ hours of DB surgery, migration work, and discovery pipeline analysis.

---

## State Snapshot

| Metric | Session Start | Current |
|--------|:---:|:---:|
| Countries | 90 | 90 |
| Cities | 228 | 258 |
| Orphans | 819 | 764 |
| Zero-city countries | 24 | 8 |
| manaully_reviewed | 0 | 2 |

---

## Completed Work

### 1. Country Merges & Cleanup
- Merged ОАЭ duplicates (114→107 via FK reassignment)
- Merged Индия duplicates (102→119)
- Fixed Rome (id=87): Индия,Гоа → Италия (country_id=153)
- Created Северный Кипр (country id=388, slug=`northern-cyprus`, `is_active=true`)
  - +2 cities: Лефкоша (391), Фамагуста (392)
  - Moved 15 TRNC channels from Турция/Греция to Северный Кипр

### 2. Zero-City Countries — Gap Closure
- Analyzed 84 channels across 22 zero-city countries for city tokens
- Created **22 cities** in **14 zero-city countries**, bound **25 channels**
- Countries with cities: 67→81
- Zero-city countries remaining: **8** (listed below)
- Method: extracted city names from `chat_username` and `title` via ILIKE, then created city rows + bound via `channel_cities`

### 3. Discovery Pipeline Recon (deep audit, no code changes)
- `discovery_v2.py` `_generate_queries()`: city × community word Cartesian pattern
- `discovered_chats` table exists but is **dead** (0 rows) — discovery v3 writes directly to `catalog_channels`
- Discovery CANNOT produce country-less channels (all queries derive from cities → country FK)
- Flood budget: 500 queries/day on dedicated account #3, 100 manual; 38-day full cycle
- `_store_results` skips `discovered_chats` entirely — INSERTs go straight to `catalog_channels`

### 4. Segment Axis Analysis
- 29 segments, 1935 demand words, 2234 total `segment_keywords`
- `channel_segments` table is **EMPTY** (0 rows across all 2522 channels)
- Segments are subscriber properties (via `user_subscriptions.segment_id`), not channel properties
- `_load_channel_segments()` exists in poller.py but has no data to load — pre-tagging is dead code path

### 5. Discovery Mechanism A — Dictionary Expansion
- Added 8 community-container terms to `COMMUNITY_RU` (55→63 words)
- 12 target terms total: 4 pre-existing, 8 new
- Committed + pushed (`13b601d`), worker restarted, verified clean
- Discovery cursor reset on restart (expected — hash changed with new queries)

---

## Current DB State

```
Countries:  90 (89 active + Северный Кипр)
Cities:     258
Orphans:    764
catalog_channels: 2522
auto_matched_country_id IS NULL: 0
```

### Remaining Zero-City Countries (8)

| Country | Orphans | Note |
|---------|:---:|------|
| Южная Корея | 30 | No geo tokens in username/title — needs external data |
| Доминикана | 3 | Токены есть (Punta Cana, Santo Domingo), город не создан |
| Люксембург | 1 | City=country (Люксембург) |
| Монако | 1 | City=country (Монако/Монте-Карло) |
| Макао | 1 | City=country |
| Сан-Марино | 1 | City=country |
| +2 more | 1 each | |

---

## Uncommitted / Untracked Files

### Staged (deleted):
- `app/admin/static/assets/index-CUcJhb-X.css` (D)
- `app/admin/static/assets/index-udsvTqLS.js` (D)

### Untracked:
- `app/admin/static/assets/index-C938yOEZ.css` — stale static asset
- `check_cursors.py` — one-off script, safe to keep or delete
- `docs/` — ~40 analysis text files from this session (useful reference, candidate for check-in or cleanup)
- `.pi/handoffs/`, `.rpiv/artifacts/handoffs/`

**Note:** `migrations/versions/idx_discovered_at.py` and `migrations/versions/manually_reviewed.py` are already tracked in git (clean), despite the session log mentioning them as needing commit. Verify with `git status`.

---

## Worker Status

```
Accounts:     @iraluxme, @mill_sofi — both authorized
Circuit Breakers: BOTH CLEAR
Post-ban:     active on both (post_ban_until:1, post_ban_until:2)
Hot tier:     polling
Budget:       BudgetExceeded warnings at end-of-day (normal — daily cap reached)
FloodWait:    0
Discovery v3: running on dedicated account #3 (if available)
```

---

## Quick Reference

```bash
# DB
docker compose exec -T db psql -U leadhunter -d leadhunter

# Admin panel
# Port 17421, auth via .env ADMIN_PASSWORD

# Alembic
docker compose exec -T worker alembic current

# Worker logs
docker compose logs worker --tail=50

# Redis
docker compose exec -T redis redis-cli KEYS "*"

# Auth userbot
docker compose run --rm -it worker python -m app.userbot.auth
```

---

## Hard Rules (from OPERATIONS.md — DO NOT BREAK)

1. **NEVER restart worker while it is actively polling** without explicit permission from the owner
2. **NEVER run `docker compose run/exec/restart/up -d` on prod while worker is running**
3. **Before ANY change to `poller.py`, `rate_limiter.py`, `classifier.py`, or `pool.py`:** read `OPERATIONS.md` §2 (Hard Rules) and §5 (checklist)
4. **After any Telegram API incident:** document in `OPERATIONS.md` — cause, timeline, lesson, new rules
5. **Secrets only in `.env`**
6. **Migrations must be reversible** (`downgrade()`), with `pg_dump` before applying
7. **Session log mandatory:** after each task, append to `CLAUDE.md` §8

---

## Next Session Priorities (ordered)

1. **Commit cleanup:** decide fate of ~40 `docs/` files, stale static assets, staged deletes
2. **Fill remaining 8 zero-city countries:**
   - Microstates (Люксембург, Монако, Макао, Сан-Марино): create city=country entries
   - Доминикана: create Punta Cana + Santo Domingo cities, bind channels
   - Южная Корея: needs external data source (30 orphans, no geo tokens in username/title)
3. **Discovery mechanism C:** graph exploration via Telegram channel connections (referenced channels in messages, forwarded-from links)
4. **Clean up `discovered_chats` table:** either wire it up or drop it (currently dead code)
5. **Revive `channel_segments`:** pre-tag channels with segments for future auto-direction feature

---

## Key Decisions Made This Session

- **City creation is safe and reversible:** FK is `ON DELETE RESTRICT` on `cities`, binds are via `channel_cities` M2M — no data loss risk
- **Discovery v3 is the active path:** `discovery_v2.py` in dedicated mode, `discovered_chats` table is vestigial
- **Segments are subscriber-axis, not channel-axis:** `channel_segments` empty by design until pre-tagging is revived
- **Механизм A (dictionary expansion) deployed:** vetted clean, no regressions
- **Северный Кипр treated as separate country:** 15 channels moved, `is_active=true`

---

## Files Modified (last commit + uncommitted)

**Committed (`13b601d`):**
- `app/userbot/discovery_v2.py` — +8 COMMUNITY_RU terms

**Earlier commits this session:** (see `git log --oneline` for full detail)
- Admin panel UX overhaul, unignore, discovery v3 isolation, BudgetExceeded throttling, city binding API, UNIQUE-safe city creation, migration + is_ignored filter

**Uncommitted:**
- Stale static deletes (staged)
- ~40 docs/ analysis files (untracked)
- `check_cursors.py` (untracked)
